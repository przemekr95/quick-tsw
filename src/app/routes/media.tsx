import { MediaInfo } from '../../features/media/MediaInfo';
import { MediaLayout } from '../../shared/components/MediaLayout';

export default function MediaRoute() {
  return (
    <MediaLayout>
      <MediaInfo />
    </MediaLayout>
  );
}
